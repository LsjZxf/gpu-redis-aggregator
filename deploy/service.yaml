apiVersion: v1
kind: Service
metadata:
  name: gpu-redis-aggregator
  namespace: default
spec:
  selector:
    app: gpu-redis-aggregator
  ports:
    - name: http
      port: 8080
      targetPort: 8080